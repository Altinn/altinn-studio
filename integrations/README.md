# Integrations

This directory will contain external system integrations.

## Planned Integrations

- **MCP Client**: Integration with Model Context Protocol server
- **Other external APIs**: As needed for the agentic system

Currently, Altinn Studio integration (Gitea) is implemented directly in the `frontend_api` package.