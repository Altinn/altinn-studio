# Runtime reapplies these defaults before creating panes and attaching clients.
set -g history-limit 50000
set -g mouse on
set -s focus-events on
set -s extended-keys on
# Index 99 is reserved for the Agent runtime; repeated sourcing is idempotent.
set -s terminal-features[99] 'xterm*:extkeys'
