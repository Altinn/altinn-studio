# Copy this file to .env and provide the host-owned values referenced by agent.yaml.
# The Sandbox receives only inert values in environment variables with these names.

GITHUB_TOKEN=
STUDIO_DEV_API_KEY=
