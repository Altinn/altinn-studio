# Bookingdato er frem i tid

Hent dagens dato med `current_date` og bookingdatoen fra `Booking.Dato`
(bruk `path_value`). Bruk `days_between` fra i dag til bookingdatoen:
0 eller flere dager frem gir `vurdert_ok`; dato i fortiden gir `vurdert_avslag`.
