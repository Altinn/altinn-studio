# JSON-only variant of the demo agent (no templates/ folder, no typst
# dependency): the orchestrated step publishes enrichment JSON without
# rendering a PDF.

steps:
  - name: sjekkliste-agent
    type: agent-pdf-orchestrated
    mapper: envelope
    publishTo: sjekkliste
