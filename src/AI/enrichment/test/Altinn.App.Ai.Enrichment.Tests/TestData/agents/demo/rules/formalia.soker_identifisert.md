# Søker er identifisert

Sjekk at `Innsender.Navn` og `Innsender.Foedselsnummer` finnes og er utfylt
(bruk `path_value`). Begge må være til stede for `vurdert_ok`; mangler en av
dem er status `maa_undersokes`.
