## In v9 apps

- **Language selector**: add `"showLanguageSelector": true` to `App/ui/Settings.json`. It holds the settings for every task, as a flat object with no `pages` property: `{"showLanguageSelector": true}`. Create the file if it does not exist.
