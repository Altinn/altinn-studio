## In v9 apps

- There are no `RuleConfiguration.json` or `RuleHandler.js` files, and you must not create them. Show and hide a component with a `hidden` expression.
- Compute values in a C# `IDataWriteProcessor`, not in an expression. `skill(altinn-planning)` shows how to register it.
