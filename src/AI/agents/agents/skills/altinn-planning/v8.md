## In v8 apps

- **UI folders**: each layout set in `App/ui/layout-sets.json` has a folder `App/ui/<layoutSetId>/`. A new app has one layout set, `form`, so its pages are in `App/ui/form/layouts/` and their order is in `App/ui/form/Settings.json`.
- **Headings**: use the `Header` component.
