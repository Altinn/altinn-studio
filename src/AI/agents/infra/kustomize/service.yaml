apiVersion: v1
kind: Service
metadata:
  name: altinn-ai-agents
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8071
      protocol: TCP
      name: http
  selector:
    app: altinn-ai-agents
