{
  "name": "path_value",
  "description": "Les en verdi fra søknads-JSON via dot-path med valgfri [index]. Eksempel: 'Bevillingsansvarlig.Styrer.Foedselsnummer'. Returnerer present=False hvis et mellomliggende steg mangler — bruk det for å avgjøre om dokumentasjon/felt er oppgitt eller mangler.",
  "parameters": {
    "type": "object",
    "properties": {
      "json_path": { "type": "string", "description": "Dot-path med valgfri [n]-indeks" }
    },
    "required": ["json_path"]
  }
}
