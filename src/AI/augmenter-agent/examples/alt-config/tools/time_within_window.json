{
  "name": "time_within_window",
  "description": "Sjekk om en tidsperiode [start_time, end_time] ligger fullt innenfor et tillatt vindu [window_start, window_end]. (Ikke i bruk i denne konfigurasjonen — permisjonssøknad bruker ikke klokkeslett — men implementasjonen er tilgjengelig.)",
  "parameters": {
    "type": "object",
    "properties": {
      "start_time":   { "type": "string", "description": "Periodens start, HH:MM" },
      "end_time":     { "type": "string", "description": "Periodens slutt, HH:MM" },
      "window_start": { "type": "string", "description": "Vinduets start, HH:MM" },
      "window_end":   { "type": "string", "description": "Vinduets slutt, HH:MM" }
    },
    "required": ["start_time", "end_time", "window_start", "window_end"]
  }
}
