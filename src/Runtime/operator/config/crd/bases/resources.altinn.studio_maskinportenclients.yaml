---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: maskinportenclients.resources.altinn.studio
spec:
  group: resources.altinn.studio
  names:
    kind: MaskinportenClient
    listKind: MaskinportenClientList
    plural: maskinportenclients
    singular: maskinportenclient
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MaskinportenClient is the Schema for the maskinportenclients
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MaskinportenClientSpec defines the desired state of MaskinportenClient
            properties:
              scopes:
                description: Scopes is a list of Maskinporten scopes that the client
                  should have access to
                items:
                  type: string
                type: array
            type: object
          status:
            description: MaskinportenClientStatus defines the observed state of MaskinportenClient
            properties:
              actionHistory:
                description: ActionHistory contains up to 10 recent actions, ordered
                  oldest to newest
                items:
                  description: ActionRecord represents a single executed command with
                    its result
                  properties:
                    command:
                      description: Command is the type of command that was executed
                      type: string
                    details:
                      description: Details contains command-specific information (clientId,
                        keyCount, etc.)
                      type: string
                    result:
                      description: Result describes the outcome (e.g., "created",
                        "updated", "deleted", "synced", "cleared")
                      type: string
                    timestamp:
                      description: Timestamp when the action was executed
                      format: date-time
                      type: string
                    traceId:
                      description: TraceId is the OTel trace ID for correlating with
                        observability backends
                      type: string
                  required:
                  - command
                  - result
                  - timestamp
                  type: object
                type: array
              authority:
                type: string
              clientId:
                description: ClientId is the client id of the client in Maskinporten
                  API
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              keyIds:
                items:
                  type: string
                type: array
              lastSynced:
                description: LastSynced is the timestamp of the last successful sync
                  towards Maskinporten API
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
