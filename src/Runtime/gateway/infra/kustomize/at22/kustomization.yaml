apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  - target:
      kind: IngressRoute
      name: gateway
    patch: |-
      - op: replace
        path: /spec/routes/0/match
        value: Host(`${SERVICEOWNER_ID}.apps.at22.altinn.cloud`) && PathPrefix(`/runtime/gateway/api/v1`)
