apiVersion: 1
datasources:
  - name: PostgreSQL
    type: grafana-postgresql-datasource
    url: postgres:5432
    user: postgres
    editable: true
    secureJsonData:
      password: postgres123
    jsonData:
      database: workflow_engine
      sslmode: disable
      maxOpenConns: 5
      maxIdleConns: 2
      connMaxLifetime: 14400
