apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: studio-gateway
  namespace: default
subjects:
  - kind: ServiceAccount
    name: studio-gateway
    namespace: studio-gateway
roleRef:
  kind: Role
  name: studio-gateway
  apiGroup: rbac.authorization.k8s.io