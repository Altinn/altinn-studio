apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: studio-gateway-helmrelease-reader
rules:
  - apiGroups:
      - helm.toolkit.fluxcd.io
    resources:
      - helmreleases
    verbs:
      - get