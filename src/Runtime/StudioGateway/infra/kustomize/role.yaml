apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: studio-gateway
  namespace: default
rules:
  - apiGroups:
      - helm.toolkit.fluxcd.io
    resources:
      - helmreleases
    verbs:
      - get