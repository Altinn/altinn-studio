apiVersion: v1
kind: Service
metadata:
  name: workflow-engine-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: workflow-engine-app
