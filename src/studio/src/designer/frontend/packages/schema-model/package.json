{
  "name": "@altinn/schema-model",
  "description": "JSON schema internal model",
  "version": "0.1.0",
  "dependencies": {
    "jsonpointer": "5.0.1"
  },
  "devDependencies": {
    "jest": "29.3.1"
  },
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "depcheck": "echo Checking $npm_package_name && npx depcheck && echo ",
    "test": "jest --maxWorkers=50%"
  }
}
