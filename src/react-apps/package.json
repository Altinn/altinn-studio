{
  "name": "root",
  "private": true,
  "scripts": {
    "install-deps": "lerna bootstrap --hoist",
    "no-ci-hoist": "lerna bootstrap --hoist --no-ci",
    "test": "lerna exec -- npm test",
    "install-all": "lerna exec -- npm i",
    "clean-all": "lerna exec -- rm package-lock.json"
  },
  "devDependencies": {
    "lerna": "3.10.7"
  }
}
