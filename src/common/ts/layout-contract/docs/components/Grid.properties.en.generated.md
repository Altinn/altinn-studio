The component also supports the common properties [`id`](../common-properties/#id), [`hidden`](../common-properties/#hidden), [`grid`](../grid/), and [`pageBreak`](../page-break/).

| Property                                              | Type                                     | Required | Default  | Description                                                                                                                                                                         |
| ----------------------------------------------------- | ---------------------------------------- | -------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `renderAsSummary`                                     | `boolean`                                | No       | `false`  | Boolean value indicating if the component should be rendered as a summary. Defaults to false.                                                                                       |
| `forceShowInSummary`                                  | `boolean \| expression<boolean>`         | No       | `false`  | Will force show the component in a summary even if hideEmptyFields is set to true in the summary component.                                                                         |
| `labelSettings`                                       | `object`                                 | No       |          | Controls how the component label is displayed.                                                                                                                                      |
| `labelSettings.optionalIndicator`                     | `boolean`                                | No       |          | Show optional indicator on label                                                                                                                                                    |
| `type`                                                | `"Grid"`                                 | Yes      |          | Identifies which component type this configuration represents.                                                                                                                      |
| `textResourceBindings`                                | `object`                                 | No       |          | Connects component texts to text resources or expressions.                                                                                                                          |
| `textResourceBindings.summaryTitle`                   | `string \| expression<string>`           | No       |          | Title used in the summary view (overrides the default title)                                                                                                                        |
| `textResourceBindings.summaryAccessibleTitle`         | `string \| expression<string>`           | No       |          | Title used for aria-label on the edit button in the summary view (overrides the default and summary title)                                                                          |
| `textResourceBindings.title`                          | `string \| expression<string>`           | No       |          | Label text/title shown above the component                                                                                                                                          |
| `textResourceBindings.description`                    | `string \| expression<string>`           | No       |          | Label description shown above the component, below the title                                                                                                                        |
| `textResourceBindings.help`                           | `string \| expression<string>`           | No       |          | Help text shown in a tooltip when clicking the help button                                                                                                                          |
| `rows`                                                | `object[]`                               | Yes      |          | The list of rows in this grid                                                                                                                                                       |
| `rows[].header`                                       | `boolean`                                | No       | `false`  |                                                                                                                                                                                     |
| `rows[].readOnly`                                     | `boolean`                                | No       | `false`  |                                                                                                                                                                                     |
| `rows[].columnOptions`                                | `object`                                 | No       |          | Options for the row/column                                                                                                                                                          |
| `rows[].columnOptions.width`                          | `string`                                 | No       | `"auto"` | Width of cell in % or 'auto'. Defaults to 'auto'                                                                                                                                    |
| `rows[].columnOptions.alignText`                      | `"left" \| "center" \| "right"`          | No       |          | Choose text alignment between 'left', 'center', or 'right' for text in table cells. Defaults to 'left' for text and 'right' for numbers. Allowed values: "left", "center", "right". |
| `rows[].columnOptions.textOverflow`                   | `object`                                 | No       |          |                                                                                                                                                                                     |
| `rows[].columnOptions.textOverflow.lineWrap`          | `boolean`                                | No       | `true`   | Toggle line wrapping on or off. Defaults to true                                                                                                                                    |
| `rows[].columnOptions.textOverflow.maxHeight`         | `number`                                 | No       | `2`      | Determines the number of lines to display in table cell before hiding the rest of the text with an ellipsis (...). Defaults to 2.                                                   |
| `rows[].columnOptions.hidden`                         | `boolean \| expression<boolean>`         | No       | `false`  | Expression or boolean indicating whether each column should be hidden. An expression will be evaluated per column, and if it evaluates to true, the column will be hidden.          |
| `rows[].cells`                                        | `(object \| null \| object \| object)[]` | Yes      |          | The list of cells in this row                                                                                                                                                       |
| `rows[].cells[].width`                                | `string`                                 | No       | `"auto"` | Width of cell in % or 'auto'. Defaults to 'auto'                                                                                                                                    |
| `rows[].cells[].alignText`                            | `"left" \| "center" \| "right"`          | No       |          | Choose text alignment between 'left', 'center', or 'right' for text in table cells. Defaults to 'left' for text and 'right' for numbers. Allowed values: "left", "center", "right". |
| `rows[].cells[].textOverflow`                         | `object`                                 | No       |          |                                                                                                                                                                                     |
| `rows[].cells[].textOverflow.lineWrap`                | `boolean`                                | No       | `true`   | Toggle line wrapping on or off. Defaults to true                                                                                                                                    |
| `rows[].cells[].textOverflow.maxHeight`               | `number`                                 | No       | `2`      | Determines the number of lines to display in table cell before hiding the rest of the text with an ellipsis (...). Defaults to 2.                                                   |
| `rows[].cells[].hidden`                               | `boolean \| expression<boolean>`         | No       | `false`  | Expression or boolean indicating whether each column should be hidden. An expression will be evaluated per column, and if it evaluates to true, the column will be hidden.          |
| `rows[].cells[].component`                            | `string`                                 | No       |          | ID of the component                                                                                                                                                                 |
| `rows[].cells[].columnOptions`                        | `object`                                 | No       |          | Options for the row/column                                                                                                                                                          |
| `rows[].cells[].columnOptions.width`                  | `string`                                 | No       | `"auto"` | Width of cell in % or 'auto'. Defaults to 'auto'                                                                                                                                    |
| `rows[].cells[].columnOptions.alignText`              | `"left" \| "center" \| "right"`          | No       |          | Choose text alignment between 'left', 'center', or 'right' for text in table cells. Defaults to 'left' for text and 'right' for numbers. Allowed values: "left", "center", "right". |
| `rows[].cells[].columnOptions.textOverflow`           | `object`                                 | No       |          |                                                                                                                                                                                     |
| `rows[].cells[].columnOptions.textOverflow.lineWrap`  | `boolean`                                | No       | `true`   | Toggle line wrapping on or off. Defaults to true                                                                                                                                    |
| `rows[].cells[].columnOptions.textOverflow.maxHeight` | `number`                                 | No       | `2`      | Determines the number of lines to display in table cell before hiding the rest of the text with an ellipsis (...). Defaults to 2.                                                   |
| `rows[].cells[].columnOptions.hidden`                 | `boolean \| expression<boolean>`         | No       | `false`  | Expression or boolean indicating whether each column should be hidden. An expression will be evaluated per column, and if it evaluates to true, the column will be hidden.          |
| `rows[].cells[].cellStyle`                            | `object`                                 | No       |          | Additional properties for columns in the Grid component                                                                                                                             |
| `rows[].cells[].cellStyle.colSpan`                    | `number \| expression<number>`           | No       |          | Number of columns this cell should span. Defaults to 1 if not set.                                                                                                                  |
| `rows[].cells[].width`                                | `string`                                 | No       | `"auto"` | Width of cell in % or 'auto'. Defaults to 'auto'                                                                                                                                    |
| `rows[].cells[].alignText`                            | `"left" \| "center" \| "right"`          | No       |          | Choose text alignment between 'left', 'center', or 'right' for text in table cells. Defaults to 'left' for text and 'right' for numbers. Allowed values: "left", "center", "right". |
| `rows[].cells[].textOverflow`                         | `object`                                 | No       |          |                                                                                                                                                                                     |
| `rows[].cells[].textOverflow.lineWrap`                | `boolean`                                | No       | `true`   | Toggle line wrapping on or off. Defaults to true                                                                                                                                    |
| `rows[].cells[].textOverflow.maxHeight`               | `number`                                 | No       | `2`      | Determines the number of lines to display in table cell before hiding the rest of the text with an ellipsis (...). Defaults to 2.                                                   |
| `rows[].cells[].hidden`                               | `boolean \| expression<boolean>`         | No       | `false`  | Expression or boolean indicating whether each column should be hidden. An expression will be evaluated per column, and if it evaluates to true, the column will be hidden.          |
| `rows[].cells[].text`                                 | `string`                                 | Yes      |          | Text to display (can also be a key in text resources)                                                                                                                               |
| `rows[].cells[].help`                                 | `string`                                 | No       |          | Help text to display                                                                                                                                                                |
| `rows[].cells[].columnOptions`                        | `object`                                 | No       |          | Options for the row/column                                                                                                                                                          |
| `rows[].cells[].columnOptions.width`                  | `string`                                 | No       | `"auto"` | Width of cell in % or 'auto'. Defaults to 'auto'                                                                                                                                    |
| `rows[].cells[].columnOptions.alignText`              | `"left" \| "center" \| "right"`          | No       |          | Choose text alignment between 'left', 'center', or 'right' for text in table cells. Defaults to 'left' for text and 'right' for numbers. Allowed values: "left", "center", "right". |
| `rows[].cells[].columnOptions.textOverflow`           | `object`                                 | No       |          |                                                                                                                                                                                     |
| `rows[].cells[].columnOptions.textOverflow.lineWrap`  | `boolean`                                | No       | `true`   | Toggle line wrapping on or off. Defaults to true                                                                                                                                    |
| `rows[].cells[].columnOptions.textOverflow.maxHeight` | `number`                                 | No       | `2`      | Determines the number of lines to display in table cell before hiding the rest of the text with an ellipsis (...). Defaults to 2.                                                   |
| `rows[].cells[].columnOptions.hidden`                 | `boolean \| expression<boolean>`         | No       | `false`  | Expression or boolean indicating whether each column should be hidden. An expression will be evaluated per column, and if it evaluates to true, the column will be hidden.          |
| `rows[].cells[].cellStyle`                            | `object`                                 | No       |          | Additional properties for columns in the Grid component                                                                                                                             |
| `rows[].cells[].cellStyle.colSpan`                    | `number \| expression<number>`           | No       |          | Number of columns this cell should span. Defaults to 1 if not set.                                                                                                                  |
| `rows[].cells[].width`                                | `string`                                 | No       | `"auto"` | Width of cell in % or 'auto'. Defaults to 'auto'                                                                                                                                    |
| `rows[].cells[].alignText`                            | `"left" \| "center" \| "right"`          | No       |          | Choose text alignment between 'left', 'center', or 'right' for text in table cells. Defaults to 'left' for text and 'right' for numbers. Allowed values: "left", "center", "right". |
| `rows[].cells[].textOverflow`                         | `object`                                 | No       |          |                                                                                                                                                                                     |
| `rows[].cells[].textOverflow.lineWrap`                | `boolean`                                | No       | `true`   | Toggle line wrapping on or off. Defaults to true                                                                                                                                    |
| `rows[].cells[].textOverflow.maxHeight`               | `number`                                 | No       | `2`      | Determines the number of lines to display in table cell before hiding the rest of the text with an ellipsis (...). Defaults to 2.                                                   |
| `rows[].cells[].hidden`                               | `boolean \| expression<boolean>`         | No       | `false`  | Expression or boolean indicating whether each column should be hidden. An expression will be evaluated per column, and if it evaluates to true, the column will be hidden.          |
| `rows[].cells[].labelFrom`                            | `string`                                 | Yes      |          | Set this to a component id to display the label from that component                                                                                                                 |
| `rows[].cells[].columnOptions`                        | `object`                                 | No       |          | Options for the row/column                                                                                                                                                          |
| `rows[].cells[].columnOptions.width`                  | `string`                                 | No       | `"auto"` | Width of cell in % or 'auto'. Defaults to 'auto'                                                                                                                                    |
| `rows[].cells[].columnOptions.alignText`              | `"left" \| "center" \| "right"`          | No       |          | Choose text alignment between 'left', 'center', or 'right' for text in table cells. Defaults to 'left' for text and 'right' for numbers. Allowed values: "left", "center", "right". |
| `rows[].cells[].columnOptions.textOverflow`           | `object`                                 | No       |          |                                                                                                                                                                                     |
| `rows[].cells[].columnOptions.textOverflow.lineWrap`  | `boolean`                                | No       | `true`   | Toggle line wrapping on or off. Defaults to true                                                                                                                                    |
| `rows[].cells[].columnOptions.textOverflow.maxHeight` | `number`                                 | No       | `2`      | Determines the number of lines to display in table cell before hiding the rest of the text with an ellipsis (...). Defaults to 2.                                                   |
| `rows[].cells[].columnOptions.hidden`                 | `boolean \| expression<boolean>`         | No       | `false`  | Expression or boolean indicating whether each column should be hidden. An expression will be evaluated per column, and if it evaluates to true, the column will be hidden.          |
| `rows[].cells[].cellStyle`                            | `object`                                 | No       |          | Additional properties for columns in the Grid component                                                                                                                             |
| `rows[].cells[].cellStyle.colSpan`                    | `number \| expression<number>`           | No       |          | Number of columns this cell should span. Defaults to 1 if not set.                                                                                                                  |
