/**
 * Viewport breakpoints used by TypeScript.
 * Keep these values in sync with breakpoints.module.css in this directory.
 */
export const breakpoints = {
  sm: 600,
  md: 1024,
  lg: 1440,
} as const;
