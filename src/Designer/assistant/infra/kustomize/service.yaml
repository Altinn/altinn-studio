apiVersion: v1
kind: Service
metadata:
  name: altinn-altinity-agents
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8071
      protocol: TCP
      name: http
  selector:
    app: altinn-altinity-agents
