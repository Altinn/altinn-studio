apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  {%- for app in apps %}
  - ../apps/{{ app }}
  {%- endfor %}

patches:
  - target:
      kind: OCIRepository
    patch: |-
      - op: replace
        path: /spec/ref/tag
        value: {{ environment }}
