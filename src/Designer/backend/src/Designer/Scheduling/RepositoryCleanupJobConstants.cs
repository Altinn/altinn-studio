namespace Altinn.Studio.Designer.Scheduling;

public static class RepositoryCleanupJobConstants
{
    public const string JobName = nameof(RepositoryCleanupJob);
    public const string TriggerName = $"{JobName}Trigger";
}
