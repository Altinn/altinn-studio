apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  - ../apps/deploy-gitops-test

patches:
  - target:
      kind: OCIRepository
    patch: |-
      - op: replace
        path: /spec/ref/tag
        value: at22
