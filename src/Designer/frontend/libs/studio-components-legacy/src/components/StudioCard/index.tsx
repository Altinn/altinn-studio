export { type CardProps as StudioCardProps } from '@digdir/designsystemet-react';
import { Card } from '@digdir/designsystemet-react';

/**
 * @deprecated Use `StudioCard` from `@studio/components` instead.
 */
export const StudioCard = Card;
