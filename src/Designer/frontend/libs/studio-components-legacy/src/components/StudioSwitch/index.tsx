export { type SwitchProps as StudioSwitchProps } from '@digdir/designsystemet-react';
import { Switch } from '@digdir/designsystemet-react';

/**
 * @deprecated Use `StudioSwitch` from `@studio/components` instead.
 */
export const StudioSwitch = Switch;
