export enum IntRestrictionKey {
  integer = 'integer',
  exclusiveMaximum = 'exclusiveMaximum',
  exclusiveMinimum = 'exclusiveMinimum',
  maximum = 'maximum',
  minimum = 'minimum',
  multipleOf = 'multipleOf',
}
