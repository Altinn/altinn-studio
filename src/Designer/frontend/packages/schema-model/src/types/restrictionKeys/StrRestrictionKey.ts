export enum StrRestrictionKey {
  format = 'format',
  formatExclusiveMaximum = 'formatExclusiveMaximum',
  formatExclusiveMinimum = 'formatExclusiveMinimum',
  formatMaximum = 'formatMaximum',
  formatMinimum = 'formatMinimum',
  maxLength = 'maxLength',
  minLength = 'minLength',
  pattern = 'pattern',
}
