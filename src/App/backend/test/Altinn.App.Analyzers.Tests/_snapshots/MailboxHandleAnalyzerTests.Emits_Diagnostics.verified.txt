[
  {
    Id: ALTINNAPP0702,
    Title: Mailbox handle answered twice,
    Severity: Error,
    WarningLevel: 0,
    Location: {ProjectDirectory}testapp/App/additional/MailboxHandleConsumption.cs(49,14),
    Code: HandleReplies,
    HelpLink: https://docs.altinn.studio/nb/altinn-studio/v8/reference/analysis/rules/altinnapp0702,
    MessageFormat: The mailbox opened by stage '{0}' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Message: The mailbox opened by stage 'SendToArchive' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Category: Contracts
  },
  {
    Id: ALTINNAPP0702,
    Title: Mailbox handle answered twice,
    Severity: Error,
    WarningLevel: 0,
    Location: {ProjectDirectory}testapp/App/additional/MailboxHandleConsumption.cs(63,14),
    Code: ConcludeOnReplies,
    HelpLink: https://docs.altinn.studio/nb/altinn-studio/v8/reference/analysis/rules/altinnapp0702,
    MessageFormat: The mailbox opened by stage '{0}' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Message: The mailbox opened by stage 'SendToJournal' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Category: Contracts
  },
  {
    Id: ALTINNAPP0702,
    Title: Mailbox handle answered twice,
    Severity: Error,
    WarningLevel: 0,
    Location: {ProjectDirectory}testapp/App/additional/MailboxHandleConsumption.cs(80,24),
    Code: ConcludeOnReplies,
    HelpLink: https://docs.altinn.studio/nb/altinn-studio/v8/reference/analysis/rules/altinnapp0702,
    MessageFormat: The mailbox opened by stage '{0}' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Message: The mailbox opened by stage 'SendToRegistry' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Category: Contracts
  },
  {
    Id: ALTINNAPP0703,
    Title: Mailbox opened but never answered,
    Severity: Error,
    WarningLevel: 0,
    Location: {ProjectDirectory}testapp/App/additional/MailboxHandleConsumption.cs(91,104),
    Code: forgotten,
    HelpLink: https://docs.altinn.studio/nb/altinn-studio/v8/reference/analysis/rules/altinnapp0703,
    MessageFormat: Stage '{0}' opens a mailbox that nothing answers: its handle is never passed anywhere, so the messages that come back would have no handler. Answer it before the pipeline ends — with HandleReplies to carry on afterwards, or with ConcludeOnReplies to end there.,
    Message: Stage 'SendAndForget' opens a mailbox that nothing answers: its handle is never passed anywhere, so the messages that come back would have no handler. Answer it before the pipeline ends — with HandleReplies to carry on afterwards, or with ConcludeOnReplies to end there.,
    Category: Contracts
  },
  {
    Id: ALTINNAPP0702,
    Title: Mailbox handle answered twice,
    Severity: Error,
    WarningLevel: 0,
    Location: {ProjectDirectory}testapp/App/additional/MailboxHandleConsumption.cs(353,14),
    Code: HandleReplies,
    HelpLink: https://docs.altinn.studio/nb/altinn-studio/v8/reference/analysis/rules/altinnapp0702,
    MessageFormat: The mailbox opened by stage '{0}' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Message: The mailbox opened by stage 'SendAfterMerge' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Category: Contracts
  },
  {
    Id: ALTINNAPP0702,
    Title: Mailbox handle answered twice,
    Severity: Error,
    WarningLevel: 0,
    Location: {ProjectDirectory}testapp/App/additional/MailboxHandleConsumption.cs(377,14),
    Code: HandleReplies,
    HelpLink: https://docs.altinn.studio/nb/altinn-studio/v8/reference/analysis/rules/altinnapp0702,
    MessageFormat: The mailbox opened by stage '{0}' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Message: The mailbox opened by stage 'SendAfterCoalesce' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Category: Contracts
  },
  {
    Id: ALTINNAPP0702,
    Title: Mailbox handle answered twice,
    Severity: Error,
    WarningLevel: 0,
    Location: {ProjectDirectory}testapp/App/additional/MailboxHandleConsumption.cs(394,14),
    Code: HandleReplies,
    HelpLink: https://docs.altinn.studio/nb/altinn-studio/v8/reference/analysis/rules/altinnapp0702,
    MessageFormat: The mailbox opened by stage '{0}' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Message: The mailbox opened by stage 'SendInsideUsing' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Category: Contracts
  },
  {
    Id: ALTINNAPP0702,
    Title: Mailbox handle answered twice,
    Severity: Error,
    WarningLevel: 0,
    Location: {ProjectDirectory}testapp/App/additional/MailboxHandleConsumption.cs(515,31),
    Code: HandleReplies,
    HelpLink: https://docs.altinn.studio/nb/altinn-studio/v8/reference/analysis/rules/altinnapp0702,
    MessageFormat: The mailbox opened by stage '{0}' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Message: The mailbox opened by stage 'SendRethrown' is already answered by an earlier handler. Each mailbox is answered exactly once — by HandleReplies or by ConcludeOnReplies, never by both and never twice — so a second handler for the same exchange would be dead code.,
    Category: Contracts
  }
]