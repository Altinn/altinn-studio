import React from 'react';

import type { CompAlertExternal } from 'src/layout/Alert/config.generated';

export function AlertComponentNext(props: CompAlertExternal) {
  console.log('props for next Alert component:', props);
  return <div>this is a placeholder for Alert component next</div>;
}
