import type { ILayouts } from 'src/layout/layout';

interface UseCodeListsParams {
  layouts?: ILayouts;
  currentLanguage: string;
}

// export function useCodeLists(layouts?: ILayouts, currentLanguage: string) {
//   console.log(layouts);
//   console.log(currentLanguage);
//   const instance = useI
// }
