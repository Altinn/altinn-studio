import { Checkbox as DSCheckbox, useCheckboxGroup as useDSCheckboxGroup } from '@digdir/designsystemet-react';
import type { CheckboxProps } from '@digdir/designsystemet-react';

export type { CheckboxProps };
export const Checkbox = DSCheckbox;
export const useCheckboxGroup = useDSCheckboxGroup;
