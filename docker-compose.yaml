services:
  # Jaeger for tracing (optional)
  # Uncomment if you want to use Jaeger for tracing
  # jaeger:
  #   image: jaegertracing/jaeger:latest
  #   container_name: jaeger
  #   ports:
  #     - "16686:16686" # Web UI
  #     - "4318:4318"   # OTLP HTTP
  #   restart: unless-stopped
