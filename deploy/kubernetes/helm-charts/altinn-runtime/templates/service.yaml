apiVersion: v1
kind: Service
metadata:
  name: altinn-runtime
  labels:
    run: altinn-runtime
spec:
  ports:
    - port: {{ .Values.service.externalPort }}
      targetPort:  {{ .Values.image.containerPort }}
      protocol:  TCP
  selector:
    run: altinn-runtime
