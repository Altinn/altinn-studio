apiVersion: v1
kind: Service
metadata:
  name: traefik
spec:
  type: {{ .Values.service.type }}
  ports:
  {{- range $port := .Values.service.ports }}
    - protocol: {{ $port.protocol }}
      name: {{ $port.name }}
      port: {{ $port.port }}
  {{- end }}
  selector:
    app: {{ .Values.name }}
