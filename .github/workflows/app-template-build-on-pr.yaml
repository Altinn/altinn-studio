name: App Template - docker build on PR

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review, converted_to_draft, closed]
    paths:
      - 'src/App/template/**'
      - '.github/workflows/app-template-build-on-pr.yaml'
      - '!**/AGENTS.md'
      - '!**/CLAUDE.md'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  analyze:
    if: ${{ github.event_name != 'pull_request' || (github.event.action != 'closed' && !github.event.pull_request.draft) }}
    strategy:
      fail-fast: false  # one template failing must not mask the state of the other
      matrix:
        os: [ubuntu-latest]
        template: [v8, v9]
    name: Run release and docker build (${{ matrix.template }})
    runs-on: ${{ matrix.os}}
    timeout-minutes: 30
    env:
      DOTNET_HOSTBUILDER__RELOADCONFIGONCHANGE: false
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Make template release
        run: |
          rsync . -rv --exclude-from=.releaseignore --exclude-from=.gitignore app-template-release
        working-directory: src/App/template/${{ matrix.template }}/src

      - name: Docker build release
        run: |
          docker build .
        working-directory: src/App/template/${{ matrix.template }}/src/app-template-release
