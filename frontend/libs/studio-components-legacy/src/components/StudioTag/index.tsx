export { type TagProps as StudioTagProps } from '@digdir/designsystemet-react';
import { Tag } from '@digdir/designsystemet-react';

/**
 * @deprecated Use `StudioTag` from `@studio/components` instead.
 */
export const StudioTag = Tag;
