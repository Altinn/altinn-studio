import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import { Heading, Paragraph } from '@digdir/designsystemet-react';
import * as StudioRecommendedNextActionStories from './StudioRecommendedNextAction.stories';

<Meta of={StudioRecommendedNextActionStories} />

<Heading level={1} size='small'>
  StudioRecommendedNextAction
</Heading>
<Paragraph>
  StudioRecommendedNextAction is a composed component designed to present a recommended next action
  to the user. Can be used to present an action suggestion that can be either performed or skipped.
</Paragraph>

<Canvas of={StudioRecommendedNextActionStories.Preview} />
