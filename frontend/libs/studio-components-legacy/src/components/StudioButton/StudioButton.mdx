import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import { Heading, Paragraph } from '@digdir/designsystemet-react';
import * as StudioButtonStories from './StudioButton.stories';

<Meta of={StudioButtonStories} />

<Heading level={1} size='small'>
  StudioButton
</Heading>
<Paragraph>
  StudioButton is a extension of the digdir-designsystemet `Button` component that simplifies the
  usage icons and icon placement.
</Paragraph>

<Canvas of={StudioButtonStories.Preview} />
