import {Canvas, Meta} from '@storybook/blocks';
import {Heading, Paragraph} from '@digdir/design-system-react';
import * as StudioSpinnerStories from './StudioSpinner.stories';

<Meta of={StudioSpinnerStories}/>

<Heading level={1} size='small'>
  StudioSpinner
</Heading>
<Paragraph>
  StudioSpinner is a loading spinner that can be used in the Studio when loading data.
</Paragraph>

<Canvas of={StudioSpinnerStories.Preview}/>
