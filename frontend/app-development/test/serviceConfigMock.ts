import { ServiceConfig } from 'app-shared/types/ServiceConfig';

export const mockServiceDescription = 'mock-service-description';
export const mockServiceId = 'mock-service-id';
export const mockServiceName = 'mock-service-name';
export const mockRepositoryName = 'mock-repository-name';

export const serviceConfigMock: ServiceConfig = {
  serviceDescription: mockServiceDescription,
  serviceId: mockServiceId,
  serviceName: mockServiceName,
  repositoryName: mockRepositoryName,
};
