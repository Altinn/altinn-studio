apiVersion: v1
kind: Service
metadata:
  name: altinn-gitea
  labels:
    run: altinn-gitea
spec:
  ports:
    - port: {{ .Values.service.externalPort }}
      targetPort: {{ .Values.image.containerPort }}
      protocol: TCP
  selector:
    run: altinn-gitea
