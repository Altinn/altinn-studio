# the only egress should be to apps
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-egress-nginx
  namespace: default
spec:
  podSelector:
    matchLabels:
      run: altinn-loadbalancer
  policyTypes:
  - Egress
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: "default"
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: "lhci"
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: "observability"
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: "nvt"
      podSelector:
        matchLabels:
          app.kubernetes.io/name: nvt-agent-gateway
          app.kubernetes.io/component: gateway
    ports:
    - port: 8080
      protocol: TCP
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: "kube-system"
    ports:
    - port: 53
      protocol: TCP
    - port: 53
      protocol: UDP
