# Default values for gitea-org-runner.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: altinntjenestercontainerregistry.azurecr.io/altinn-gitea-runner
  # This sets the pull policy for images.
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# Set to "kata-vm-isolation" to run pods in a Kata microVM on AKS Pod Sandboxing nodes.
# Leave empty to use the default runc runtime.
runtimeClassName: ""

# Docker-in-Docker sidecar. When enabled, a privileged dockerd container is added to each
# runner pod and DOCKER_HOST is injected into the runner container pointing at the sidecar.
# Intended to be combined with runtimeClassName: kata-vm-isolation so "privileged"
# is scoped to the microVM, not the host node.
#
# On kata, /var/lib/docker cannot use the default emptyDir (virtio-fs doesn't support
# overlay as an upper filesystem). The sidecar's entrypoint therefore loop-mounts a
# sparse ext4 image file at /var/lib/docker when it detects virtio-fs. diskSize controls
# the logical size of that sparse file; physical usage grows only with actual writes.
dockerInDocker:
  enabled: false
  image:
    repository: docker
    tag: "dind"
    pullPolicy: IfNotPresent
  diskSize: "10G"

# studioctl support: mounts a shared emptyDir at STUDIOCTL_HOME in both the runner
# and the dockerd sidecar, and sets STUDIOCTL_HOME on the runner. This makes
# studioctl's bind mounts visible to dockerd across the DinD container boundary,
# since the docker client's filesystem view (runner) and the docker daemon's
# filesystem view (dockerd sidecar) are otherwise disjoint.
studioctlSupport:
  enabled: false
  home: "/altinn-studio"

# List of runners to deploy. Each runner will create a separate deployment.
runners: []
  # - name: ttd
  #   replicas: 5
  #   environmentVariables:
  #     - name: GITEA_INSTANCE_URL
  #       value: "https://gitea.example.com"
  #     - name: GITEA_RUNNER_REGISTRATION_TOKEN
  #       valueFrom:
  #         secretKeyRef:
  #           name: gitea-runner-ttd-token
  #           key: token
  #     - name: GITEA_RUNNER_NAME
  #       value: "ttd-runner"
  # - name: brg
  #   replicas: 3
  #   environmentVariables:
  #     - name: GITEA_INSTANCE_URL
  #       value: "https://gitea.example.com"
  #     - name: GITEA_RUNNER_REGISTRATION_TOKEN
  #       valueFrom:
  #         secretKeyRef:
  #           name: gitea-runner-brg-token
  #           key: token
  #     - name: GITEA_RUNNER_NAME
  #       value: "brg-runner"

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext:
  fsGroup: 1000

# securityContext:
#   privileged: false

  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000


resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
