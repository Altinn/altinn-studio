digdir:
  event:
    logging:
      feature-enabled: true
      environment-name: dev
      bootstrap-servers:
      schema-registry-url:
      kafka-username:
      kafka-password:
      schema-registry-username:
      schema-registry-password:
