apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: nvt-platform
  namespace: default
spec:
  interval: 5m
  path: ./platform
  prune: true
  sourceRef:
    kind: OCIRepository
    name: nvt-agent-config
    namespace: default
  timeout: 2m
  wait: true
