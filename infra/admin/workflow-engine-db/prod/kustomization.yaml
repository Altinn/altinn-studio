apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  - target:
      kind: DatabaseServer
      name: workflow-engine-db-server
    patch: |-
      - op: replace
        path: /spec/serverType
        value: prod
