# LLM API Keys
AZURE_API_KEY=your_azure_api_key_here
OPENAI_API_KEY=your_openai_api_key_here # OPTIONAL: Azure is used by default.

# Gitea Access Token
#
# SINGLE-TENANT MODE (Local development):
# Set this to your personal Gitea API token
GITEA_API_KEY=your_gitea_api_key_here
#
# MULTI-TENANT MODE (Kubernetes/Production):
# Leave GITEA_API_KEY empty or unset
# Tokens will be passed per-request via Authorization header:
# Authorization: Bearer <user_gitea_token>  (OAuth 2.0 style)
# Authorization: token <user_gitea_token>   (Gitea/GitHub style)
#
# The server automatically detects which mode to use:
# 1. If Authorization header is present -> uses that token (multi-tenant)
# 2. If no header but GITEA_API_KEY is set -> uses env token (single-tenant)
# 3. If neither is available -> returns error
